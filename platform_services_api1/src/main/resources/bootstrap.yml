eureka:
  client: 
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/

spring: 
  application:
    name: api1
  cloud: 
    config:
      name: api1
      label: master
      discovery:
        enabled: true
        service-id: CONFIGURATION